# DEPRECATED — use pyproject.toml + uv instead.
# See sync.bat or run: uv sync --extra tts-edge
#
# Legacy pip install (installs everything):
#   pip install -r requirements.txt
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
openai>=1.30.0
pyyaml>=6.0
faster-whisper>=1.0.0
edge-tts>=7.0.0
